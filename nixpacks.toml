[phases.setup]
nixPkgs = ['php82', 'php82Packages.composer', 'nodejs-18_x', 'npm-9_x']

[phases.install]
cmds = [
    'chmod +x deploy.sh',
    './deploy.sh'
]

[phases.build]
cmds = []

[start]
cmd = 'chmod +x start.sh && ./start.sh'